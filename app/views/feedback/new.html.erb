<h1><%= t("shared.header.feedback") %></h1>
<%= form_with url: submit_feedback_index_path, local: true do |f| %>
  <div class="row form-group">
    <%=f.label :title, t(".title"), class: "col-sm-2 col-form-label" %>
    <%=f.text_field :title, class: "form-control col col-lg-4" %>
  </div>
  <div class="row form-group">
    <%=f.label :body, t(".body"), class: "col-sm-2 col-form-label" %>
    <%=f.text_area :body, class: "form-control col col-lg-4" %>
  </div>
  <div class="actions">
    <%= f.submit t(".send"), class: "btn btn-primary" %>
  </div>
<% end %>