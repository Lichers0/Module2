<tr>
  <td><%= question.id  %></td>
  <td><%= question.body %></td>
  <td><%= link_to t("common.show"), question_path(question)  %></td>
  <td><%= link_to t("common.edit"), edit_question_path(question) %></td>
  <td><%= link_to t("common.delete"),
                  question_path(question),
                  method: :delete,
                  data: { confirm: t("common.r_u_sure") }  %></td>
</tr>